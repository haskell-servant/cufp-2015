version: "0.1"

ghc-options: -Wall

dependencies:
  - base
  - bytestring
  - containers
  - servant-server
  - servant-docs
  - aeson
  - string-conversions
  - transformers
  - either
  - wai
  - cufp-api

executables:
  reference-node:
    main: Main.hs
    source-dirs:
      - driver
      - src
    dependencies:
      - warp
      - logging-facade

tests:
  spec:
    main: Spec.hs
    source-dirs:
      - test
      - src
    dependencies:
      - hspec
      - hspec-wai
      - hspec-wai-json
      - wai-extra
